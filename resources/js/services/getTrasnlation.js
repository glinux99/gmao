export function getTranslations() {
    const translations = [];

    //   for (const key in i18n.messages) {
    //     translations.push({
    //       key,
    //       value: i18n.t(key),
    //     });
    //   }

    return translations;
}
